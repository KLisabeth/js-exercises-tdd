var evenNumbers = require("./get-even-numbers");

test("Get even number", function() {
    var input = [22, 13, 73, 82, 4];
    var output = evenNumbers(input);
    
    expect(output).toEqual(output);
});
// example
// input: [22, 13, 73, 82, 4];
// expected: [22, 82, 4];
var evenNumbers = require("./get-even-numbers");

test("Get even number", function() {
    var input = [22, 13, 73, 82, 4];
    var output = evenNumbers(input);
    
    expect(output).toEqual(output);
});